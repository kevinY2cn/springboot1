#日志设置
logging:
  config: classpath:logback.xml
  
#服务器设置
server:
  port: 1111
  
#数据源设置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/server?characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false
    username: test1
    password: 123456
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

#mybatis
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*Mapper.xml
    
        